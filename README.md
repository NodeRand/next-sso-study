# Next.js의 SSO 로그인 구현 스터디\_ver2

Next.js의 SSO 로그인 관련 기능 구현 스터디 레포지토리
<br>

## 배포 url

[]
<br>

## 사용법

1. `git clone .` 으로 클론 작업 수행
2. `npm install` 로 `package.json`파일에 따른 의존성 패키지 설치
3. 사내 자료를 통해 루트 폴더에서 `.env`파일 작성 후 생성
4. `npm run dev` 실행 후 `localhost:3000`에서 작업 가능
